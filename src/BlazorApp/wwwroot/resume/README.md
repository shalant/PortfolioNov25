# Resume

Drop the resume PDF here as:

```
Douglas-Rosenberg-Resume.pdf
```

That exact filename is what `About.razor`'s "Download Resume" button links to
(`resume/Douglas-Rosenberg-Resume.pdf`). Until the file is added, the link 404s.

Delete this README once the real PDF is in place — it's just a placeholder so the
`resume/` folder exists in git (empty folders aren't tracked).
